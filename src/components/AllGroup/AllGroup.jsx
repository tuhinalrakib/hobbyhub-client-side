import React from 'react';

const promise = fetch('https://hobby-hub-server-ten.vercel.app/groups').then(res=>res.json())

const AllGroup = () => {
    return (
        <div>
            
        </div>
    );
};

export default AllGroup;